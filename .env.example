# Subject model — the LLM being evaluated for risk
SUBJECT_MODEL=your-subject-model-name
SUBJECT_MODEL_PATH=/path/to/models/your-subject-model.gguf

# Judge model — the LLM that scores subject responses
JUDGE_MODEL=your-judge-model-name
JUDGE_MODEL_PATH=/path/to/models/your-judge-model.gguf
